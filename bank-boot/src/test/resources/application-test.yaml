spring:
  datasource:
    url: jdbc:h2:mem:banktest;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    # H2 tests: skip PostgreSQL-only migrations (e.g. PL/pgSQL triggers under db/migration/postgresql).
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  health:
    redis:
      enabled: false

bank:
  logging:
    loki:
      enabled: false
    logstash:
      enabled: false
    access:
      enabled: false
  rate-limiting:
    enabled: false
  http:
    cors:
      allowed-origins: []
  iam:
    redis:
      enabled: false
  payments:
    redis-idempotency: false
  loans:
    internal-funding-account-id: aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa
  notifications:
    dev-redirect-email: ""
    stub-sms-e164: "+15555550100"
    dispatch-mode: direct
    kafka:
      enabled: false
