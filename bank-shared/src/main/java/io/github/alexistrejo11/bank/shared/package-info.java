/**
 * Shared kernel for the modular monolith: value objects, identifiers, results, domain event base types,
 * HTTP response envelopes, and global exception mapping. Domain modules depend on this package only;
 * they do not reference each other’s internals.
 */
package io.github.alexistrejo11.bank.shared;
